# Local overrides — Vite loads this AFTER .env, takes precedence.
# Switch to the local render engine for visual testing.
# DO NOT commit this file — it's in .gitignore.
VITE_RENDER_API_URL=http://127.0.0.1:42002
VITE_RENDER_API_TOKEN=local-dev-token
